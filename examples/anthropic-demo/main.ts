// 用途:Anthropic Claude 集成 demo —— provider:'anthropic' 走 Claude 原生协议(动态 import @langchain/anthropic)
import { createApp } from 'vue'
import App from './App.vue'
import '../_shared/theme.css'

createApp(App).mount('#app')
