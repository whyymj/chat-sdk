1. tools等是否已经抽离了？如果是，examples/ 示例中是否需要修改？应该是引入tools模块，手动注入agents与subagents吧
2. 有哪些通用底层tools，middleware或者其他功能需要内置，不需要分离？主agent与子agents需必须的tools或者功能要内置，避免影响正常使用
3. 对于subagents，todos等功能是否需要system prompt中默认一些提示词？除了用户自定义的system prompt，是否需要默认拼接一些提示词，保证提升部分功能的正确使用率
4. 聊天框agent，subagents信息要与日志调试窗口分开。需要看到subagents的调试信息也要包括：skills,tools,mcp信息等
5. 聊天框没有看到终止，重试等按键，还要展示可用的mcp服务，tools等信息
6. 当前agent主要出发点是window属性修改功能，这个功能的tools集需要一起打包，但要单独引入，注入使用；这是主要业务，后续还会开发其他业务的工具集
7. 自带的聊天UI组件，抽离出来，作为独立的模块，方便快速使用又能替换自定义组件；也一起打包发布，但是手动引入注入使用
8. 自带聊天框样式过于ai风格化，优化一下，要更加美观，简洁
9. 数据回退功能是如何使用的？